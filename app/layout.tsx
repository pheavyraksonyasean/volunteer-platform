import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "VolunteerMatch - Connect Volunteers with Meaningful Causes",
  description:
    "Join thousands of volunteers and organizations making a difference in their communities. Find opportunities that match your skills and passion.",
  keywords: "volunteer, volunteering, community service, nonprofit, charity, social impact",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
